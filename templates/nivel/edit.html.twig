{% extends 'base.html.twig' %}

{% block title %}Editar Nivel{% endblock %}

{% block body %}
<div class="card-body p-5 bg-white rounded">
    <h1>EDITAR NIVEL</h1>

    {{ include('nivel/_form.html.twig', {'button_label': 'Editar'}) }}

    <a href="{{ path('nivel_index') }}">Regresar a la lista</a>
{% if is_granted("ROLE_SUPER_ADMIN") %}
    {{ include('nivel/_delete_form.html.twig') }}
    {% endif %}
    </div>
    
{% endblock %}
