<form method="post" action="{{ path('modulos_delete', {'id': modulo.id}) }}" onsubmit="return confirm('¿Está seguro de que desea eliminar?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modulo.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
