<form method="post" action="{{ path('comprobantes_delete', {'id': comprobante.id}) }}" onsubmit="return confirm('¿Está seguro de eliminar este registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comprobante.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
