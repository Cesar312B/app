{% extends 'base.html.twig' %}

{% block title %}Grupo{% endblock %}

{% block body %}
    <h1>GRUPO</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ grupo.id }}</td>
            </tr>
            <tr>
                <th>NOMBRE</th>
                <td>{{ grupo.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grupo_index') }}">Regresar</a>

    <a href="{{ path('grupo_edit', {'id': grupo.id}) }}">Editar</a>

    {{ include('grupo/_delete_form.html.twig') }}
{% endblock %}
