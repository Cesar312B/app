{% extends 'base.html.twig' %}

{% block title %}Editar Grupo{% endblock %}

{% block body %}
<div class="card-body p-5 bg-white rounded">
    <h1>EDITAR GRUPO</h1>

    {{ include('grupo/_form.html.twig', {'button_label': 'Editar'}) }}

    <a href="{{ path('grupo_index') }}">Regresar a la lista</a>
    
{% if is_granted("ROLE_SUPER_ADMIN") %}
    {{ include('grupo/_delete_form.html.twig') }}
    {% endif %}
    </div>
    
{% endblock %}
