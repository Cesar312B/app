{% extends 'base.html.twig' %}

{% block title %}Nueva materia{% endblock %}

{% block body %}
    <h1>CREAR NUEVA MATERIA</h1>


   {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}


    <a href="{{ path('materia_index') }}">Regresar</a>
{% endblock %}
