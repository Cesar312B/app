{% extends 'base.html.twig' %}

{% block title %}Materia{% endblock %}

{% block body %}
    <h1>MATERIA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ materium.id }}</td>
            </tr>
            <tr>
                <th>Materia</th>
                <td>{{ materium.Materia }}</td>
            </tr>
            <tr>
                <th>HORA</th>
                <td>{{ materium.Hora }}</td>
            </tr>
            <tr>
                <th>DURACIÓN</th>
                <td>{{ materium.Duracion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('materia_index') }}">Regresar</a>

    <a href="{{ path('materia_edit', {'id': materium.id}) }}">Editar</a>

    {{ include('materia/_delete_form.html.twig') }}
{% endblock %}
