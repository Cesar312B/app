{% extends 'base.html.twig' %}

{% block title %} Estudiante{% endblock %}

{% block body %}
  
  <h5>
   {{ materias.Materia }}
  </h5>
   

    <div class="p-5 bg-white rounded shadow mb-5">     
    
    <a href="{{ path('materia_teacher') }}">Regresar</a>
         <div class="table-responsive">
        <table id="example1" class="table table-striped table-bordered" >
        <thead>
               <tr>
                <th>ESTUDIANTE</th>
                <th>MATERIA</th>
                 <th>ACCIONES</th>
            </tr>
        </thead>
         <tbody>
        {% for nota in notas %}
            <tr>
            <td>{{ nota.Nombre }} {{ nota.Apellido }}</td>
               <td>{{ nota.Materia }}</td>
         
                  <td><a href="{{ path('ast', {'id':nota.CD}) }}">ASISTENCIA</a> </td>
                  
            </tr>
        {% else %}
            <tr>
                <td colspan="18">NO EXISTE REGISTROS</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    
      </div>
     -->
  </div>




{% endblock %}
