<form method="post" action="{{ path('carrera_delete', {'id': carrera.id}) }}" onsubmit="return confirm('¿Está seguro de eliminar este registro?.Si elimina este registro se borrará toda la información relacionada a esta carrera.');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrera.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
